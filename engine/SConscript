Import('env')
Import('star', 'projection', 'gl_setup', 'events', 'game')

# build target
env.Program('engine', ['main.cpp', 'defines.cpp', 'error.cpp', 'timing.cpp'] + star + projection + gl_setup + events + game)
